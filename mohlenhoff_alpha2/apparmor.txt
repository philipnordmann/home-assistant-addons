#include <tunables/global>

profile mohlenhoff_alpha2 flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Capabilities
  file,
  signal (send) set=(kill,term,int,hup,cont),

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to options.json and other files within your addon
  /data/** rw,

  # Start new profile for service
  /usr/bin/alpha2_integration.py cx -> alpha2,

  profile alpha2 flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    #include <abstractions/python>

    # Receive signals from parent
    signal (receive) peer=mohlenhoff_alpha2,

    # Application file access
    /usr/bin/alpha2_integration.py rix,
    /usr/bin/alpha2_client.py r,
    
    # Libraries and modules
    /usr/lib/python3* r,
    /usr/local/lib/python3* r,
    /usr/local/lib/python3*/dist-packages/** r,
    /usr/lib/python3*/dist-packages/** r,
    
    # Network access
    network inet stream,
    network inet6 stream,
    
    # Common system libraries
    /usr/lib/x86_64-linux-gnu/** rm,
    /usr/lib/@{multiarch}/lib*.so* rm,
  }
}